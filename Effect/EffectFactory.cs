using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EffectFactory
{
    // public GameObject CreateEffect(string effectPath, Vector3 spawnPosition, Vector3 targetPosition)
    // {
    //     var resource = Resources.Load<GameObject>(effectPath);
    //     if (resource == null) return null;
    //     
    //     var effectPrefab =
    //         Object.Instantiate(resource, spawnPosition, Quaternion.identity);
    //     var effect = effectPrefab.GetComponent<EffectBase>();
    //     if (effect == null) return null;
    //     
    //     effect. = targetPosition;
    //     return effectPrefab;
    // }
}
